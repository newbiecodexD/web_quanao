@model web_quanao.ViewModels.CategoryFormViewModel
@{
    ViewBag.Title = "S?a danh m?c";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}
@using (Html.BeginForm(null, null, FormMethod.Post, new { @class="js-submit" })){
    @Html.AntiForgeryToken()
    @Html.HiddenFor(m => m.CategoryId)
    <div class="mb-3">
        @Html.LabelFor(m => m.Name, "Tên danh m?c")
        @Html.TextBoxFor(m => m.Name, new { @class = "form-control", placeholder="Nh?p tên" })
        @Html.ValidationMessageFor(m => m.Name)
    </div>
    <div class="mb-3">
        @Html.LabelFor(m => m.Description, "Mô t?")
        @Html.TextAreaFor(m => m.Description, 4, 60, new { @class = "form-control", placeholder="Mô t? ng?n" })
    </div>
    <button class="btn btn-primary" data-loading>L?u</button>
    <a class="btn btn-secondary" href="@Url.Action("Index")" data-loading>H?y</a>
}
@section Scripts{ @Scripts.Render("~/bundles/jqueryval") }
